% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggsave_tiff.R
\name{ggsave_tiff}
\alias{ggsave_tiff}
\title{Save ggplot as High Resolution TIFF with LZW Compression}
\usage{
ggsave_tiff(
  filename,
  path = NULL,
  plot = ggplot2::last_plot(),
  width = NA,
  height = NA,
  units = c("in", "cm", "mm", "px"),
  dpi = 300,
  compression = "lzw",
  scale = 1,
  limitsize = FALSE,
  ...
)
}
\arguments{
\item{filename}{File name to create on disk. Should have a \code{.tif} or
\code{.tiff} extension.}

\item{path}{Path of the directory to save plot to. If \code{NULL} (default),
uses the current working directory.}

\item{plot}{Plot to save. Defaults to last plot displayed.}

\item{width, height}{Plot size in \code{units} (defaults to the size of the
current graphics device).}

\item{units}{One of "in" (inches, default), "cm", "mm", or "px" (pixels).}

\item{dpi}{Plot resolution. Defaults to 300 DPI for publication quality.}

\item{compression}{Compression type for TIFF. Defaults to "lzw" for
lossless compression. Other options include "none", "rle", "jpeg",
"deflate", or "zip".}

\item{scale}{Multiplicative scaling factor. Defaults to 1.}

\item{limitsize}{When \code{TRUE}, ggsave will not save images larger
than 50x50 inches to prevent common errors.}

\item{...}{Other arguments passed on to \code{\link[ragg:agg_tiff]{ragg::agg_tiff()}} and
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Invisibly returns the filename, following \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}
convention.
}
\description{
A wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} that uses the ragg device to save
plots as high-resolution TIFF files with LZW compression. This format is
commonly required for scientific publications.
}
\examples{
\dontrun{
library(ggplot2)

# Create a simple plot
p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_minimal()

# Save as high-res TIFF with default settings (300 DPI, LZW compression)
ggsave_tiff("myplot.tiff", plot = p, width = 6, height = 4)

# Save with custom DPI
ggsave_tiff("myplot_600dpi.tiff", plot = p, width = 6, height = 4, dpi = 600)

# Save with different compression
ggsave_tiff("myplot_zip.tiff", plot = p, width = 6, height = 4, 
            compression = "zip")

# Save last displayed plot (no need to specify plot argument)
print(p)
ggsave_tiff("lastplot.tiff", width = 8, height = 6)
}

}
\references{
Wickham H (2016). ggplot2: Elegant Graphics for Data Analysis.
Springer-Verlag New York. ISBN 978-3-319-24277-4,
\url{https://ggplot2.tidyverse.org}.
}
\seealso{
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}, \code{\link[ragg:agg_tiff]{ragg::agg_tiff()}}
}
