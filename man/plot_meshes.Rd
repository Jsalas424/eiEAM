% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_meshes.R
\name{plot_meshes}
\alias{plot_meshes}
\title{Plot multiple cardiac meshes with LAT, CV, and optional point cloud data}
\usage{
plot_meshes(
  mesh_list,
  show_wireframe = TRUE,
  opacity = 0.9,
  lat_data = NULL,
  cv_data = NULL,
  pointcloud_data = NULL
)
}
\arguments{
\item{mesh_list}{Named list of \code{mesh3d} objects.}

\item{show_wireframe}{Logical; overlay wireframe if TRUE. Default TRUE.}

\item{opacity}{Mesh opacity. Default 0.9.}

\item{lat_data}{Named list of data.frames, each with column \code{lat}
(length = n vertices of corresponding mesh).}

\item{cv_data}{Named list of data.frames, each with columns \code{u, v, w}
(length = n vertices of corresponding mesh). If \code{x,y,z} are not
supplied, vertex coords are retrieved from the mesh.}

\item{pointcloud_data}{Named list of data.frames, each with \code{x,y,z}.}
}
\value{
A plotly 3D figure object with all meshes overlaid in a single scene.
}
\description{
Plot multiple cardiac meshes with LAT, CV, and optional point cloud data
}
\examples{
\dontrun{
# Example: base mesh with wireframe
plot_meshes(list(example = sample_mesh))

# Example with LAT coloring
df_lat <- list(example = data.frame(lat = rnorm(ncol(sample_mesh$vb))))
plot_meshes(list(example = sample_mesh), lat_data = df_lat)

# Example with cone vectors
df_cv <- list(example = data.frame(
  u = rnorm(ncol(sample_mesh$vb)),
  v = rnorm(ncol(sample_mesh$vb)),
  w = rnorm(ncol(sample_mesh$vb))
))
plot_meshes(list(example = sample_mesh), cv_data = df_cv)
}

}
