% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_wireframe_mesh.R
\name{create_wireframe_mesh}
\alias{create_wireframe_mesh}
\title{Create a wireframe mesh}
\usage{
create_wireframe_mesh(mesh3d_obj)
}
\arguments{
\item{mesh3d_obj}{A mesh3d object from the rgl package}
}
\value{
A data frame with x, y, z coordinates for wireframe edges compatible with plotly
}
\description{
Meshing and remeshing is central to voltage and conduction velocity. This tool allows us to visualize the mesh in plotly.
}
\examples{
# Only run if rgl is available
if (requireNamespace("rgl", quietly = TRUE)) {
  # Create a simple triangular mesh (tetrahedron)
  vertices <- rbind(
    c(0, 0, 0),    # vertex 1
    c(1, 0, 0),    # vertex 2
    c(0.5, 1, 0),  # vertex 3
    c(0.5, 0.5, 1) # vertex 4
  )

  faces <- rbind(
    c(1, 2, 3),    # bottom face
    c(1, 2, 4),    # front face
    c(2, 3, 4),    # right face
    c(1, 3, 4)     # left face
  )

  # Create mesh3d object
  test_mesh <- rgl::tmesh3d(
    vertices = t(vertices),
    indices = t(faces),
    homogeneous = FALSE
  )

  # Generate wireframe coordinates
  wireframe_coords <- create_wireframe_mesh(test_mesh)
  print(head(wireframe_coords))
}

}
